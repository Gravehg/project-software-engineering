import { Routes } from '@angular/router';
import { AdminTicketsComponent } from './admin-tickets.component';
export const ADMINTICKETS: Routes = [
  { path: '', component: AdminTicketsComponent },
];
